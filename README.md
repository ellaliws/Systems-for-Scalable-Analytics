# Systems-for-Scalable-Analytics

This project conducts data engineering for the Amazon dataset using Apache Spark. It is divided into 2 parts. The extracted features in Part 1 will be used for the Part 2, which trains a model (or models) to predict user ratings for a product.
The default Spark API will be DataFrame, as it is now the recommended choice over the RDD API
